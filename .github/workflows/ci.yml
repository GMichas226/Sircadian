name: CI

# Build the host tests and run them on every push and pull request, then
# compile every Arduino example for AVR and ESP32. Warnings are treated as
# errors (-Werror) so the build stays clean.

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --parallel

      - name: Test
        run: ctest --test-dir build --output-on-failure

  arduino-examples:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - fqbn: esp32:esp32:esp32
            platforms: |
              - name: esp32:esp32
                source-url: https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          - fqbn: arduino:avr:mega
            platforms: |
              - name: arduino:avr
    steps:
      - uses: actions/checkout@v4
      - uses: arduino/compile-sketches@v1
        with:
          fqbn: ${{ matrix.fqbn }}
          platforms: ${{ matrix.platforms }}
          libraries: |
            - source-path: ./
            - name: OneWire
            - name: DallasTemperature
          sketch-paths: |
            - examples
          enable-warnings-report: true
